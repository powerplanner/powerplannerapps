<interfacesViews:ViewHostGeneric
    x:Name="ThisPage"
    xmlns:interfacesViews="using:InterfacesUWP.Views"
    x:Class="PowerPlannerUWP.Views.SettingsViews.CalendarIntegrationClassesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:data="using:PowerPlannerAppDataLibrary">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.Transitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.Transitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="1" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
            <StackPanel Margin="20">
                <TextBlock
                    Text="{x:Bind data:R.S('Settings_CalendarIntegration_Classes_Header.Text')}"
                    Style="{ThemeResource TitleTextBlockStyle}"
                    Margin="0,0,0,12"/>

                <ToggleSwitch
                    x:Name="ToggleSwitchIsEnabled"
                    IsOn="{x:Bind ViewModel.IsIntegrationEnabled, Mode=TwoWay}"/>

                <TextBlock
                    Text="{x:Bind data:R.S('Settings_CalendarIntegration_Classes_Description.Text')}"
                    Margin="0,3,0,0"
                    Style="{ThemeResource BodyTextBlockStyle}"/>


            </StackPanel>
        </ScrollViewer>
    </Grid>
</interfacesViews:ViewHostGeneric>