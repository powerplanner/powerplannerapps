<UserControl
    x:Name="ThisView"
    x:Class="InterfacesUWP.ViewModelPresenters.PopupsPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InterfacesUWP.ViewModelPresenters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:BareMvvm.Core.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Visibility="Collapsed"
    xmlns:controls="using:InterfacesUWP.Controls">

    <Grid>

        <Rectangle
            x:Name="RectangleBackdrop"
            Fill="Black"
            Opacity="0.3"
            Tapped="RectangleBackdrop_Tapped"/>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListView ItemsSource="{x:Bind ViewModel.Popups, Mode=OneWay}" VerticalAlignment="Top"/>

            <ItemsControl x:Name="ItemsControlPopups" ItemsSource="{x:Bind ViewModel.Popups, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:BaseViewModel">
                        <local:GenericViewModelPresenter ViewModel="{x:Bind}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <controls:PopupsPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <Border
                x:Name="KeyboardOcclusion"
                Grid.Row="1"/>
        </Grid>

    </Grid>

</UserControl>
